# SpacePingBot

Telegram-бот для пинга серверов/веб сайтов и проверки задержки/доступности TCP-портов

Текущие возможности:
- Пинг сервера по ICMP
- Проверка доступности TCP-порта
- Получение данных WHOIS для домена/IP-адреса
- Получение информации о Minecraft Java/Bedrock сервере
- Получение информации о Source/GoldSource сервере

## Конфигурация:

```yaml
bot:
  token: "token" # <- Токен бота Telegram

settings:
  usages: 3 # <- Кулдаун: кол-во использований до лимита
  seconds: 5 # <- Кулдаун: длительность кулдауна

nodes: # <- Ноды
  - name: "Тестовая нода" # <- Имя ноды
    address: "http://localhost:8002" # <- Адрес до АПИ ноды
  - name: "Хельсинки нода"
    address: "http://95.216.165.24:8002"
lang: # <- Сообщения пользователю
  start: # <- Сообщение при команде /start
    - "Привет, {user_name}!"
    - ""
    -  "Я @SpacePingBot — бот, позволяющий получить краткую сводку о веб-сервисе. Могу пропинговать, проверить порты,
     получить информацию о Minecraft-сервере, IP или доменном имени."
    - ""
    -  "Вот список доступных команд:"
    - ""
    - " 📌 /ping <host> — пропинговать сервер/сайт"
    - " 📌 /tcp <host> <port> — проверить TCP-порт"
    - ""
    - " 📌 /web <host> — проверить сайт по HTTP"
    - " 📌 /whois <host> — узнать владельца IP/домена"
    - ""
    - " 📌 /mc java/bedrock <host> <port> — проверить сервер Minecraft"
    - " 📌 /source <host> <port> — проверить сервер на движке Source/GoldSource"
    - ""
  common: # <- Общие сообщения
    restricted: # <- Сообщение при наложении кулдауна
      - "Подождите 5 секунд, прежде чем использовать бота снова!"
    unrestricted: # <- Сообщение при окончании кулдауна
      - "Вы снова можете использовать бота!"
    restriction: # <- Сообщение при продолжении кулдауна
      - "Вы всё ещё не можете использовать бота!"
    invalid-host: # <- Сообщение при отправке недопустимого IP/домена
      - "Неверный хост!"
    invalid-port: # <- Сообщение при отправке недопустимого порта
      - "Неверный порт!"
    invalid-command: # <- Сообщение при использовании несуществующей команды
      - "Неизвестная команда! Введите /start для просмотра доступных команд!"
  whois: # <- Сообщения команды /whois
    format: # <- Сообщение при неверном использовании команды
      - "Использование: /whois host (/whois google.com)"
    header: # <- Шапка отчёта о проверке
      - "Результаты проверки WHOIS по адресу: {host}"
      - "Дата проверки: {date} {time} ({timezone})"
    footer: # <- Подвал отчёта о проверке
      - "\nПроверка завершена!"
    additional: # <- Информация о сервере (под подвалом)
      - "Информация WHOIS:"
      - "Домен: {domain}"
      - "Идентификатор: {registry_domain_id}"
      - "Организация: {registry_domain_id}"
      - "Адрес сайта регистратора: {registrar_url}"
      - "Дата последних изменений: {updated_date}"
      - "Дата регистрации: {creation_date}"
      - "Окончание срока регистрации: {expires_date}"
      - "Имя регистратора: {registrar_name}"
      - "IANA индентификатор: {registrar_iana_id}"
      - "Электронный адрес для жалоб: {registrar_abuse_contact_email}"
      - "Телефон для жалоб: {registrar_abuse_contact_phone}"
      - "Адреса электронной почты: {emails}"
      - "Телефонные номера: {phone_numbers}"
      - "Номера факс: {fax_numbers}"
      - "Стутус домена: {status}"
      - "DNS-Серверы: {name_servers}"
  src: # <- Сообщения команды /src
    format: # <- Сообщение при неверном использовании команды
      - "Использование: /src host port (/src some.server.com 27015)"
    header: # <- Шапка отчёта о проверке
      - "Результаты проверки Source сервера: {host}:{port}"
      - "Дата проверки: {date} {time} ({timezone})"
    footer: # <- Подвал отчёта о проверке
      - "\nПроверка завершена!"
    additional: # <- Информация о сервере (под подвалом)
      - "Информация о сервере:"
      - "Название: {server_name}"
      - "Протокол: {protocol}"
      - "Игра: {game}"
      - "Версия: {version}"
      - "Онлайн: {player_count}/{max_players}"
      - "VAC-Защита: {vac_enabled}"
      - "Пароль для входа: {password_protected}"
      - "Теги: {keywords}"
    node: # <- Сообщения от нод
      success: # <- Успешный пинг
        - "\n{num}. {node_name}"
        - "Задержка: {ping} мс"
      fail: # <- Провальный пинг
        - "\n{num}. {node_name}"
        - "Ошибка подключения\n"
  mc: # <- Сообщения команды /mc
    format: # <- Сообщения команды /mc
      - "Использование: /mc java/bedrock host port (/mc java hypixel.net 25565)"
    invalid-type: # <- Сообщение при отправке неверного типа сервера
      - "Неверный тип сервера (Возможно значение java/bedrock)"
    header: # <- Шапка отчёта о проверке
      - "Результаты проверки Minecraft {type} сервера: {host}:{port}"
      - "Дата проверки: {date} {time} ({timezone})"
    footer: # <- Подвал отчёта о проверке
      - "\nПроверка завершена!"
    additional: # <- Информация о сервере (под подвалом)
      java: # <- Для JAVA серверов
        - "Информация о сервере:"
        - "Название: {motd}"
        - "Версия: {version}"
        - "Онлайн: {online}/{max}"
      bedrock: # <- Для BEDROCK серверов
        - "Информация о сервере:"
        - "Название: {motd}"
        - "Версия: {version}"
        - "Онлайн: {online}/{max}"
        - "Карта: {map}"
        - "Режим: {gamemode}"
    node: # <- Сообщения от нод
      success: # <- Успешный пинг
        - "\n{num}. {node_name}"
        - "Задержка: {ping} мс"
      fail: # <- Провальный пинг
        - "\n{num}. {node_name}"
        - "Ошибка подключения\n"
  web: # <- Сообщения команды /web
    format: # <- Сообщение при неверном использовании команды
      - "Использование: /web host (/web google.com)"
    header: # <- Шапка отчёта о проверке
      - "Результаты проверки WEB хоста: {host}"
      - "Дата проверки: {date} {time} ({timezone})"
    footer: # <- Подвал отчёта о проверке
      - "\nПроверка завершена!"
    node: # <- Сообщения от нод
      success: # <- Успешный пинг
        - "\n{num}. {node_name}"
        - "Отправлено|получено пакетов: {packets_sent}|{packets_received} (Потеря {packet_loss})"
        - "Минимальная|Средняя|Максимальная задержка: {rtts} мс"
      fail: # <- Провальный пинг
        - "\n{num}. {node_name}"
        - "Ошибка подключения\n"
  ping: # <- Сообщения команды /ping
    format: # <- Сообщение при неверном использовании команды
      - "Использование: /ping host (/ping google.com)"
    header: # <- Шапка отчёта о проверке
      - "Результаты пинга хоста: {host}"
      - "Дата проверки: {date} {time} ({timezone})"
    footer: # <- Подвал отчёта о проверке
      - "\nПроверка завершена!"
    node: # <- Сообщения от нод
      success: # <- Успешный пинг
        - "\n{num}. {node_name}"
        - "Отправлено|получено пакетов: {packets_sent}|{packets_received} (Потеря {packet_loss})"
        - "Минимальная|Средняя|Максимальная задержка: {rtts} мс"
      fail: # <- Провальный пинг
        - "\n{num}. {node_name}"
        - "Ошибка подключения\n"
  tcp: # <- Сообщения команды /tcp
    format: # <- Сообщение при неверном использовании команды
      - "Использование: /tcp ip port (/tcp 127.0.0.1 80)"
    header: # <- Шапка отчёта о проверке
      - "Результаты проверки TCP порта: {host}:{port}"
      - "Дата проверки: {date} {time} ({timezone})"
    footer: # <- Подвал отчёта о проверке
      - "\nПроверка завершена!"
    node: # <- Сообщения от нод
      success: # <- Успешный пинг
        open: # <- Открытый порт
          - "\n{num}. {node_name}"
          - "Порт доступен! Сервис: {service}"
        closed: # <- Закрытый порт
          - "\n{num}. {node_name}"
          - "Порт недоступен\n"
      fail: # <- Провальный пинг
        - "\n{num}. {node_name}"
        - "Ошибка подключения\n"
```

## Установка

- Запустить/Создать сервер на OC Linux/Windows
- Установить `Python 3.12` и последнюю доступную версию `pip`
- Загрузить папку `SpacePingBot` и её содержимое на сервер
- Выполнить команды `cd SpacePingBot` и `pip3 install -r requirements.txt`
- В файле конфигурации `SpacePingBot/config/config.yml` поставить токен
- В файле конфигурации `SpacePingBot/config/config.yml` настроить ноды, их названия и адреса
- Выполнить команды `cd SpacePingBot` и `python3 main.py` (или предварительно запустить screen)